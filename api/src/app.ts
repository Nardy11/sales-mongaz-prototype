import { randomUUID } from "node:crypto";
import cookie from "@fastify/cookie";
import cors from "@fastify/cors";
import rateLimit from "@fastify/rate-limit";
import Fastify from "fastify";
import { z } from "zod";
import { AuditService } from "./audit";
import { AuthService, requireAuth, requireCsrf, requireRole } from "./auth";
import type { IdentityRepository } from "./repository";
import { CustomerCommitmentService } from "./customer-commitment";
import { RepresentativeService } from "./representative";

export const createApi = async ({ repository, sales, representative, allowedOrigin = "http://localhost:5173" }: { repository: IdentityRepository; sales?: CustomerCommitmentService; representative?: RepresentativeService; allowedOrigin?: string }) => {
  const app = Fastify({ logger: false, requestIdHeader: "x-request-id", genReqId: () => randomUUID() });
  const audit = new AuditService(repository); const auth = new AuthService(repository, audit);
  await app.register(cookie); await app.register(cors, { origin: allowedOrigin, credentials: true }); await app.register(rateLimit, { global: false });
  app.addHook("preHandler", async (request) => { request.correlationId = request.id; request.auth = await auth.authenticate(request); });
  app.setErrorHandler((error, request, reply) => reply.code(error.statusCode ?? 500).send({ error: "REQUEST_FAILED", message: error.statusCode && error.statusCode < 500 ? error.message : "Unexpected server error.", correlationId: request.correlationId }));
  app.get("/health", async (request) => ({ status: "ok", service: "sales-api", correlationId: request.correlationId }));
  app.post("/api/auth/login", { config: { rateLimit: { max: 8, timeWindow: "1 minute" } } }, async (request, reply) => { const input = z.object({ email: z.email(), password: z.string().min(12).max(256) }).parse(request.body); const result = await auth.login(input.email, input.password, reply, request.correlationId); if (!result) return reply.code(401).send({ error: "INVALID_CREDENTIALS", message: "Invalid email or password." }); return { employee: { id: result.employee.id, displayName: result.employee.displayName, role: result.employee.role }, csrfToken: result.csrfToken }; });
  app.get("/api/auth/me", { preHandler: requireAuth }, async (request) => ({ employee: request.auth!.employee, csrfToken: request.auth!.csrfToken }));
  app.post("/api/auth/logout", { preHandler: [requireAuth, requireCsrf] }, async (request, reply) => { await auth.logout(request, reply); return reply.code(204).send(); });
  app.get("/api/foundation/role-check", { preHandler: [requireAuth, requireRole(["sales_manager"])] }, async (request) => ({ role: request.auth!.employee.role, allowed: true }));
  const actor = (request: any) => ({ id: request.auth!.employee.id, organizationId: request.auth!.employee.organizationId, teamId: request.auth!.employee.teamId, role: request.auth!.employee.role });
  const salesReady = (reply: any) => { if (!sales) { reply.code(503).send({ error: "SALES_STORE_UNAVAILABLE", message: "Persistent sales storage is required." }); return false; } return true; };
  app.get("/api/customers", { preHandler: requireAuth }, async (request:any,reply) => { if(!salesReady(reply)) return; return sales!.listCustomers(actor(request), (request.query as any)?.q); });
  app.get("/api/customers/:id", { preHandler: requireAuth }, async (request:any,reply) => { if(!salesReady(reply)) return; const result=await sales!.customerDetail(actor(request),request.params.id); return result ?? reply.code(404).send({error:"NOT_FOUND",message:"Customer is not accessible."}); });
  app.post("/api/customers", { preHandler: [requireAuth, requireCsrf] }, async (request:any,reply) => { if(!salesReady(reply)) return; const input=z.object({name:z.string().min(2).max(160),customerCode:z.string().max(50).optional(),classification:z.enum(["gold","silver","follow_up"]),operationalStatus:z.enum(["normal","attention","risk"]).optional(),isActive:z.boolean().optional(),contactName:z.string().max(120).optional(),phone:z.string().max(40).optional(),email:z.email().optional(),address:z.string().max(250).optional(),city:z.string().max(100).optional(),operationalNotes:z.string().max(1000).optional(),ownerEmployeeId:z.string().uuid().optional()}).parse(request.body); return reply.code(201).send(await sales!.createCustomer(actor(request),input,request.correlationId)); });
  app.patch("/api/customers/:id", { preHandler: [requireAuth, requireCsrf] }, async (request:any,reply) => { if(!salesReady(reply)) return; const input=z.object({name:z.string().min(2).max(160).optional(),customerCode:z.string().max(50).optional(),classification:z.enum(["gold","silver","follow_up"]).optional(),operationalStatus:z.enum(["normal","attention","risk"]).optional(),isActive:z.boolean().optional(),contactName:z.string().max(120).optional(),phone:z.string().max(40).optional(),email:z.email().optional(),address:z.string().max(250).optional(),city:z.string().max(100).optional(),operationalNotes:z.string().max(1000).optional(),ownerEmployeeId:z.string().uuid().optional()}).refine((value)=>Object.keys(value).length>0).parse(request.body); return sales!.updateCustomer(actor(request),request.params.id,input,request.correlationId); });
  app.post("/api/commitments", { preHandler: [requireAuth, requireCsrf] }, async (request:any,reply) => { if(!salesReady(reply)) return; const input=z.object({customerId:z.string().uuid(),ownerEmployeeId:z.string().uuid().optional(),kind:z.enum(["follow_up","customer_care","internal"]),title:z.string().min(2).max(180),dueAt:z.string().datetime(),urgency:z.enum(["normal","caution","urgent"]).optional(),sourceType:z.string().max(80).optional(),sourceId:z.string().max(100).optional(),sourceEvidence:z.string().max(1000).optional(),idempotencyKey:z.string().uuid()}).parse(request.body); return reply.code(201).send(await sales!.createCommitment(actor(request),input,request.correlationId)); });
  app.post("/api/commitments/:id/complete", { preHandler: [requireAuth, requireCsrf] }, async (request:any,reply) => { if(!salesReady(reply)) return; const input=z.object({evidence:z.string().min(2).max(1000),resulting:z.object({ownerEmployeeId:z.string().uuid().optional(),kind:z.enum(["follow_up","customer_care","internal"]),title:z.string().min(2).max(180),dueAt:z.string().datetime(),urgency:z.enum(["normal","caution","urgent"]).optional(),idempotencyKey:z.string().uuid()}).optional()}).parse(request.body); return sales!.completeCommitment(actor(request),request.params.id,input,request.correlationId); });
  const repReady=(reply:any)=>{if(!representative){reply.code(503).send({error:"SALES_STORE_UNAVAILABLE",message:"Persistent sales storage is required."});return false;}return true;}; const repOnly=(request:any)=>{if(request.auth!.employee.role!=="sales_representative")throw Object.assign(new Error("Representative role required."),{statusCode:403});};
  app.get("/api/representative/day",{preHandler:requireAuth},async(request:any,reply)=>{if(!repReady(reply))return;repOnly(request);return representative!.day(actor(request));});
  app.get("/api/products",{preHandler:requireAuth},async(request:any,reply)=>{if(!repReady(reply))return;repOnly(request);return representative!.products(actor(request));});
  app.post("/api/customers/:id/reactivation",{preHandler:[requireAuth,requireCsrf]},async(request:any,reply)=>{if(!repReady(reply))return;repOnly(request);const input=z.object({evidence:z.string().min(2).max(1000),title:z.string().min(2).max(180),dueAt:z.string().datetime()}).parse(request.body);return reply.code(201).send(await representative!.reactivate(actor(request),request.params.id,input,request.correlationId));});
  app.post("/api/visits",{preHandler:[requireAuth,requireCsrf]},async(request:any,reply)=>{if(!repReady(reply))return;repOnly(request);const input=z.object({customerId:z.string().uuid(),plannedAt:z.string().datetime(),purpose:z.string().min(2),idempotencyKey:z.string().uuid()}).parse(request.body);return reply.code(201).send(await representative!.createVisit(actor(request),input,request.correlationId));});
  app.post("/api/visits/:id/start",{preHandler:[requireAuth,requireCsrf]},async(request:any,reply)=>{if(!repReady(reply))return;repOnly(request);return representative!.startVisit(actor(request),request.params.id,request.correlationId);});
  app.post("/api/visits/:id/complete",{preHandler:[requireAuth,requireCsrf]},async(request:any,reply)=>{if(!repReady(reply))return;repOnly(request);const input=z.object({outcome:z.string().min(2),evidence:z.string().min(2),notes:z.string().optional(),followUpTitle:z.string().min(2).optional(),followUpDueAt:z.string().datetime().optional()}).parse(request.body);return representative!.completeVisit(actor(request),request.params.id,input,request.correlationId);});
  app.post("/api/orders",{preHandler:[requireAuth,requireCsrf]},async(request:any,reply)=>{if(!repReady(reply))return;repOnly(request);const input=z.object({customerId:z.string().uuid(),visitId:z.string().uuid().optional(),productId:z.string().uuid(),quantity:z.number().positive(),unitPrice:z.number().nonnegative().optional(),requestNote:z.string().optional(),duplicateOverrideReason:z.string().min(2).optional()}).parse(request.body);return reply.code(201).send(await representative!.createOrder(actor(request),input,request.correlationId));});
  for(const type of ["opportunity","complaint","observation","collection"] as const)app.post(`/api/${type}s`,{preHandler:[requireAuth,requireCsrf]},async(request:any,reply)=>{if(!repReady(reply))return;repOnly(request);const input=z.object({customerId:z.string().uuid(),visitId:z.string().uuid().optional(),kind:z.string().min(1).optional(),productReference:z.string().optional(),note:z.string().optional(),classification:z.string().optional(),description:z.string().optional(),responsibleParty:z.string().optional(),requiredAction:z.string().optional(),observationType:z.string().optional(),competitor:z.string().optional(),competitorPrice:z.number().nonnegative().optional(),offer:z.string().optional(),outcome:z.enum(["collected","partial","promise","no_collection"]).optional(),amountCollected:z.number().nonnegative().optional(),promiseAmount:z.number().positive().optional(),promiseDueAt:z.string().datetime().optional(),evidence:z.string().optional(),followUpTitle:z.string().optional(),followUpDueAt:z.string().datetime().optional()}).parse(request.body);return reply.code(201).send(await representative!.capture(actor(request),type,input,request.correlationId));});
  return app;
};
