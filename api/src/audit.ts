import type { IdentityRepository } from "./repository";
import { newAuditEvent } from "./repository";
import type { Sql } from "postgres";
export class AuditService { constructor(private readonly repository: IdentityRepository) {} async record(input: Parameters<typeof newAuditEvent>[0]) { const event = newAuditEvent(input); await this.repository.writeAudit(event); return event; } async recordInTransaction(tx: Sql,input: Parameters<typeof newAuditEvent>[0]) { const event=newAuditEvent(input); await tx`INSERT INTO audit_events (id,actor_employee_id,action,resource_type,resource_id,before_data,after_data,reason,correlation_id,created_at) VALUES (${event.id},${event.actorId},${event.action},${event.resourceType},${event.resourceId},${event.before},${event.after},${event.reason},${event.correlationId},${event.createdAt})`; return event; } }
